# Tunnel secrets — never commit
cloudflared/creds.json
cloudflared/*.json
*.pem
.env
contact.env

# Contact-form submissions (stored at runtime — never commit customer messages)
data/

# basic-auth credential for /admin (never commit)
web/.htpasswd

# python bytecode
__pycache__/
*.pyc

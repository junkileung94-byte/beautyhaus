<div class="wrap page-body">
  <header class="coll-head">
    <h1>{{ page.title }}</h1>
  </header>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
